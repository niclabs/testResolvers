language: go

before_install:
  - go get github.com/miekg/dns
  - go get golang.org/x/crypto/ssh
  - go install ./github.com/niclabs/testResolvers/config
  - go install ./github.com/niclabs/testResolvers/resolvertests

notifications:
  email:
    on_success: never
    on_failure: never #always/change/never
